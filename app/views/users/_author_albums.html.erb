<table class="table album_title_table"> 
 	<% @albums.each{|album| %>
 	<% album_name = album["title"] %>
 	<tr>
 		<td>
  			<%= link_to "#{album_name}",album_info_path(album['id']),:class => "read_m" %>
  		</td>
 	</tr>
 	<% } %>
</table>